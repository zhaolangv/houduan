# 快速部署指南

## 🚀 5分钟快速部署到Supabase

### 前置条件

- ✅ Supabase账号（免费）
- ✅ GitHub账号
- ✅ Railway账号（或Render/Fly.io）

---

## ⚡ 快速检查：是否已配置？

如果你已经配置过Supabase，可以跳过配置步骤，直接部署！

### 检查配置

运行检查脚本：

```bash
python check_deployment.py
```

如果看到所有检查都通过 ✅，说明配置已完成，可以直接跳到 **[步骤3: 部署到Railway](#步骤3-部署到railway3分钟)**

### 已配置的项

从你的 `.env` 文件中，我看到你已经配置了：

- ✅ `DATABASE_URL` - Supabase数据库连接字符串
- ✅ `SUPABASE_URL` - Supabase项目URL  
- ✅ `SUPABASE_ANON_KEY` - Supabase匿名密钥
- ✅ `SUPABASE_STORAGE_BUCKET` - 存储桶名称

**如果配置正确，可以直接跳到部署步骤！** 🎉

---

## 步骤1: 配置Supabase（5分钟）

> **注意**：如果你已经配置好了，可以跳过此步骤，直接看步骤3。

### 1.1 创建Supabase项目

### 1.1 创建Supabase项目

1. 访问 https://supabase.com
2. 创建新项目
3. **记住数据库密码**（重要！）

### 1.2 获取连接信息

在Supabase Dashboard中：

1. **数据库连接字符串**：
   - Settings → Database → Connection string
   - 选择 **Session pooler** 模式
   - 复制连接字符串
   - **替换** `[YOUR-PASSWORD]` 为实际密码

2. **API密钥**：
   - Settings → API
   - 复制 **Project URL** 和 **anon public key**

3. **创建Storage存储桶**：
   - Storage → New bucket
   - 名称：`question-images`
   - ✅ 勾选 Public bucket

---

## 步骤2: 配置环境变量（2分钟）

> **注意**：如果你已经配置好了 `.env` 文件，可以跳过此步骤，直接看步骤3。

### 2.1 创建.env文件

如果还没有 `.env` 文件，复制 `env.example` 为 `.env`：

```bash
cp env.example .env
```

### 2.2 编辑.env文件

填入以下配置：

```env
# 数据库（替换为你的实际值）
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres

# Supabase Storage
SUPABASE_URL=https://[PROJECT-REF].supabase.co
SUPABASE_ANON_KEY=你的anon public key
SUPABASE_STORAGE_BUCKET=question-images

# AI配置（DeepSeek）
AI_PROVIDER=deepseek
AI_API_KEY=你的DeepSeek API Key
AI_API_BASE=https://api.deepseek.com/v1
AI_MODEL=deepseek-chat
```

### 2.3 验证配置

运行检查脚本：

```bash
python check_deployment.py
```

确保所有检查通过 ✅

---

## 步骤3: 部署到Railway（3分钟）

### 3.1 推送代码到GitHub

> **详细步骤**：如果这是你的第一个GitHub仓库，或者需要详细说明，请查看 [`GitHub推送指南.md`](GitHub推送指南.md)

#### 快速步骤：

```bash
# 1. 初始化Git（如果还没有）
git init

# 2. 添加文件（.gitignore会自动排除.env等敏感文件）
git add .

# 3. 提交
git commit -m "Initial commit: 公考题库分析服务"

# 4. 在GitHub上创建新仓库（访问 https://github.com/new）
#    仓库名：gongkao-backend（或你喜欢的名称）
#    不要勾选 "Initialize with README"

# 5. 添加远程仓库并推送（替换为你的用户名和仓库名）
git remote add origin https://github.com/你的用户名/gongkao-backend.git
git branch -M main
git push -u origin main
```

**注意**：
- 如果提示 "remote origin already exists"，说明当前目录已经有Git配置，需要先删除：`git remote remove origin`
- 如果电脑上已有其他项目的Git仓库，请参考 [`GitHub推送指南.md`](GitHub推送指南.md) 的详细说明

### 3.2 创建Railway项目

1. 访问 https://railway.app
2. 使用GitHub登录
3. New Project → Deploy from GitHub repo
4. 选择你的仓库

### 3.3 配置环境变量

在Railway项目设置中，添加所有环境变量（从.env文件复制）

### 3.4 部署

Railway会自动：
- 检测Python项目
- 安装依赖
- 启动应用

完成后会提供 `https://your-app.railway.app` URL

---

## 步骤4: 验证部署（1分钟）

### 4.1 健康检查

```bash
curl https://your-app.railway.app/api/health
```

应该返回：

```json
{
  "status": "healthy",
  "checks": {
    "database": {"status": "connected"}
  }
}
```

### 4.2 测试接口

```bash
curl https://your-app.railway.app/api/test
```

---

## ✅ 完成！

你的应用已成功部署！

### 下一步

1. **配置自定义域名**（可选）
   - Railway → Settings → Domains

2. **查看日志**
   - Railway → Deployments → View Logs

3. **监控使用情况**
   - Railway Dashboard

---

## 🆘 遇到问题？

### 常见问题

1. **数据库连接失败**
   - 检查 `DATABASE_URL` 中的用户名格式（必须是 `postgres.[PROJECT-REF]`）
   - 确认密码已替换（不是 `[YOUR-PASSWORD]`）

2. **Storage上传失败**
   - 确认存储桶名称正确
   - 检查存储桶是否为公开（Public）

3. **应用启动失败**
   - 查看Railway日志
   - 确认所有环境变量已配置

### 获取帮助

- 查看详细文档：`Supabase部署指南.md`
- 运行检查脚本：`python check_deployment.py`
- 查看Supabase文档：https://supabase.com/docs

---

## 📝 环境变量清单

部署前确保以下变量已配置：

- [ ] `DATABASE_URL` - Supabase数据库连接字符串
- [ ] `SUPABASE_URL` - Supabase项目URL
- [ ] `SUPABASE_ANON_KEY` - Supabase匿名密钥
- [ ] `SUPABASE_STORAGE_BUCKET` - 存储桶名称（默认：question-images）
- [ ] `AI_PROVIDER` - AI提供商（deepseek/openai）
- [ ] `AI_API_KEY` - AI API密钥
- [ ] `AI_API_BASE` - AI API基础URL
- [ ] `AI_MODEL` - AI模型名称

---

**祝部署顺利！** 🎉
