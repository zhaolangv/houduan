# Supabase PostgreSQL数据库连接（必需）
# 获取方式：Supabase Dashboard → Settings → Database → Connection string (Connection pooling模式)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:6543/postgres

# Supabase存储配置（可选，用于上传图片到云端）
# 获取方式：Supabase Dashboard → Settings → API
SUPABASE_URL=https://[PROJECT-REF].supabase.co
SUPABASE_ANON_KEY=你的anon public key
# SUPABASE_SERVICE_KEY=你的service_role key（可选，用于管理操作）
# SUPABASE_STORAGE_BUCKET=question-images（可选，默认：question-images）

# AI API配置（支持DeepSeek和OpenAI）
# 方式1: 使用DeepSeek（推荐，性价比高）
AI_PROVIDER=deepseek
AI_API_KEY=sk-7de12481a17045819fcf3a2838d884a1
AI_API_BASE=https://api.deepseek.com/v1
AI_MODEL=deepseek-chat

# 方式2: 使用OpenAI（可选）
# AI_PROVIDER=openai
# AI_API_KEY=your_openai_api_key_here
# AI_API_BASE=https://api.openai.com/v1
# AI_MODEL=gpt-4-vision-preview

# 兼容旧配置（如果上面没配置，会尝试读取这些）
# OPENAI_API_KEY=your_api_key_here
# OPENAI_API_BASE=https://api.openai.com/v1

# Embedding模型配置（可选）
# 可选值：clip-ViT-B-32（默认，平衡速度和准确度）、clip-ViT-L-14（更准确但更慢）
EMBEDDING_MODEL=clip-ViT-B-32

# 百度OCR配置（可选，但推荐使用，准确率更高）
# 注册地址：https://ai.baidu.com/
# 每月1000次免费，准确率比PaddleOCR高很多
# 获取方式：登录百度AI开放平台 → 控制台 → 文字识别 → 创建应用
BAIDU_OCR_APP_ID=your_baidu_app_id
BAIDU_OCR_API_KEY=your_baidu_api_key
BAIDU_OCR_SECRET_KEY=your_baidu_secret_key

# 火山引擎OCR配置（可选，50万tokens免费额度）
# 注册地址：https://www.volcengine.com
# 获取方式：火山引擎控制台 → API密钥管理
VOLCENGINE_ACCESS_KEY_ID=your_access_key_id
VOLCENGINE_SECRET_ACCESS_KEY=your_secret_access_key
VOLCENGINE_REGION=cn-north-1
VOLCENGINE_USE_VISION_MODEL=true
VOLCENGINE_VISION_MODEL=doubao-lite-vision-32k

